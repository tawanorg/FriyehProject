import React from 'react';
import {SafeAreaView} from 'react-native';
// import headerHeight from 'utils/headerHeight';

const PageView = ({children}) => <SafeAreaView>{children}</SafeAreaView>;

export default PageView;
